# Environment variables for Q-Ware Agent System

# --- LLM Configuration ---
# Specify the primary LLM provider to use: "gemini" or "openai" (or others as supported)
# LLM_PROVIDER="gemini"

# Gemini Configuration (if LLM_PROVIDER is "gemini")
# Ensure you have the correct library to interface with Gemini (e.g., via Vertex AI or Google AI SDK).
# If using Google AI SDK directly, GEMINI_API_KEY is typically needed.
# If using Vertex AI, authentication is often handled via GOOGLE_APPLICATION_CREDENTIALS or gcloud login.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"
GEMINI_MODEL_NAME="gemini-1.5-flash-latest" # Or your preferred Gemini model

# OpenAI Configuration (if LLM_PROVIDER is "openai" or as a fallback)
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"
OPENAI_MODEL_NAME="gpt-4o" # Or your preferred OpenAI model

# Google Cloud Project (if using Vertex AI for Gemini or other Google Cloud services)
# GOOGLE_CLOUD_PROJECT="your-gcp-project-id"
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/gcp-service-account-key.json" # If using a service account

# --- Other Configurations (Examples) ---
# Add other environment variables your application might need, e.g.:
# DATABASE_URL="your_database_connection_string"
# LOG_LEVEL="INFO"
