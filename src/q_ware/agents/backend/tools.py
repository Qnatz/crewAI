# from crewai_tools import tool

# Example (placeholder tool for the coordinator):
# @tool
# def create_task_dependency_plan(tasks: list) -> str:
#     """Creates a dependency plan for a list of backend tasks."""
#     # Logic to analyze tasks and determine execution order or dependencies
#     return f"Dependency plan created for tasks: {tasks}"

# my_tools = [create_task_dependency_plan]
my_tools = [] # Start with no tools, can be added as needed
