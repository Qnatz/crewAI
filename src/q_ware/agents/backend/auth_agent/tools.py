# from crewai_tools import tool

# Example (placeholder tool - can be expanded later):
# @tool
# def generate_jwt_token(user_id: str, claims: dict) -> str:
#     """Generates a JWT token for a given user ID and claims."""
#     # Actual JWT generation logic would go here
#     return f"dummy_jwt_for_{user_id}_with_claims_{claims}"

# my_tools = [generate_jwt_token]
my_tools = [] # Start with no tools, can be added as needed
